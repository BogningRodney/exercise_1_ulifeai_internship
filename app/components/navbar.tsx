import { AppBar, Box, Toolbar } from '@mui/material'
import React from 'react'

export default function NavBar() {
  return (
    <AppBar position="sticky">
      <Toolbar>Nav bar</Toolbar>
    </AppBar>
  )
}
